import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // logging: {
  //   fetches: {
  //     fullUrl: true, // Shows the entire URL instead of just the path
  //   },
  //   incomingRequests: true, // Set to false to turn off those route notifications
  // },
};

export default nextConfig;
